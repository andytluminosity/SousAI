# Copy this file to `.env` (project root) and replace the placeholder
# with your real OpenAI key. A build phase syncs this file into the
# app bundle on every build — you only ever need to edit this one.
OPENAI_API_KEY=sk-replace-me
